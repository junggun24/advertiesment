import type { Metadata } from 'next';
export const metadata:Metadata={title:'디스플레이 설치사례',description:'공공청사, 안전 현장과 문화시설의 안내전광판 및 멀티비전 설치 구성을 확인하세요.',alternates:{canonical:'/cases'}};
export default function Layout({children}:{children:React.ReactNode}){return children}
