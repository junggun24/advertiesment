'use client';
import { useEffect, useRef, useState } from 'react';
import { Bold, Heading2, ImageUp, Italic, Link, List, ListOrdered } from 'lucide-react';

export function RichTextEditor({value,onChange}:{value:string;onChange:(value:string)=>void}){const editor=useRef<HTMLDivElement>(null);const savedRange=useRef<Range|null>(null);const [uploading,setUploading]=useState(false);const [message,setMessage]=useState('');useEffect(()=>{if(editor.current&&editor.current.innerHTML!==value)editor.current.innerHTML=value||'<p><br></p>'},[value]);function rememberSelection(){const selection=getSelection();if(selection?.rangeCount&&editor.current?.contains(selection.anchorNode))savedRange.current=selection.getRangeAt(0).cloneRange()}function command(name:string,argument?:string){editor.current?.focus();if(savedRange.current){const selection=getSelection();selection?.removeAllRanges();selection?.addRange(savedRange.current)}document.execCommand(name,false,argument);rememberSelection()}async function addLink(){const url=prompt('연결할 주소를 입력해 주세요.','https://');if(url&&/^https?:\/\//i.test(url))command('createLink',url)}async function upload(file?:File){if(!file)return;setUploading(true);setMessage('이미지 업로드 중...');const form=new FormData();form.set('file',file);try{const response=await fetch('/api/uploads',{method:'POST',body:form});const result=await response.json() as {url?:string;message?:string};if(!response.ok||!result.url){setMessage(result.message??'이미지를 업로드하지 못했습니다.');return}command('insertImage',result.url);onChange(editor.current?.innerHTML??'');setMessage('이미지가 본문에 삽입되었습니다.')}catch{setMessage('파일 저장소에 연결할 수 없습니다.')}finally{setUploading(false)}}return <div className="rich-editor"><div className="rich-toolbar" aria-label="본문 서식"><button type="button" title="굵게" onMouseDown={event=>{event.preventDefault();command('bold')}}><Bold/></button><button type="button" title="기울임" onMouseDown={event=>{event.preventDefault();command('italic')}}><Italic/></button><button type="button" title="제목" onMouseDown={event=>{event.preventDefault();command('formatBlock','h2')}}><Heading2/></button><button type="button" title="글머리 목록" onMouseDown={event=>{event.preventDefault();command('insertUnorderedList')}}><List/></button><button type="button" title="번호 목록" onMouseDown={event=>{event.preventDefault();command('insertOrderedList')}}><ListOrdered/></button><button type="button" title="링크" onMouseDown={event=>{event.preventDefault();void addLink()}}><Link/></button><label className={uploading?'disabled':''} onMouseDown={rememberSelection}><ImageUp/>{uploading?'업로드 중':'이미지 삽입'}<input type="file" accept="image/jpeg,image/png,image/webp,image/gif" disabled={uploading} onChange={event=>void upload(event.target.files?.[0])}/></label></div><div ref={editor} className="rich-editor-area" contentEditable suppressContentEditableWarning data-placeholder="본문을 입력하세요. 이미지를 넣으면 첫 이미지가 자동 썸네일로 사용됩니다." onInput={event=>{rememberSelection();onChange(event.currentTarget.innerHTML)}} onKeyUp={rememberSelection} onMouseUp={rememberSelection}/>{message&&<small>{message}</small>}</div>}
